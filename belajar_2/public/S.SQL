CREATE TABLE PEGAWAI (
    ID INT PRIMARY KEY,
    NAMA VARCHAR(100),
    DEPARTEMEN_ID INT,
    SUPERVISOR_ID INT REFERENCES PEGAWAI(ID)
);

CREATE TABLE DEPARTEMEN (
    ID INT PRIMARY KEY,
    NAMA VARCHAR(50),
    KEPALA_ID INT REFERENCES PEGAWAI(ID)
);

